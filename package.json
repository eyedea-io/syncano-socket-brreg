{
  "license": "MIT",
  "dependencies": {
    "axios": "^0.16.2",
    "syncano-server": "gamma"
  },
  "scripts": {
    "test": "yarn && cross-env NODE_ENV=test mocha -s 400 'test/*.test.js' --reporter spec --require babel-register --timeout 4000",
    "test:coverage": "nyc --produce-source-map=true npm run test"
  },
  "devDependencies": {
    "babel-core": "^6.26.0",
    "chai": "^4.1.2",
    "mocha": "^3.5.3",
    "syncano-test": "^0.1.5"
  },
  "babel": {
    "presets": [
      [
        "env",
        {
          "targets": {
            "node": "7"
          }
        }
      ]
    ]
  }
}
